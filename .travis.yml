os:
  - linux
  - osx
language: generic
sudo: required
osx_image: xcode10.3
xcode_project: SwiftyPyString.xcodeproj
dist: trusty
env:
  - SWIFT_VERSION=5.0
install:
  - eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"
script:
  - swift test
  - pod spec lint SwiftyPyString.podspec
